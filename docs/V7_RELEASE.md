# 金枢 V7｜发布与验收记录

2026-09-25。个人项目；产品与方案设计，AI辅助工程实现。

## 已完成的远端验收

[Actions 36194212567](https://github.com/cxy-peter/jinshu-fintech-agent/actions/runs/36194212567)，应用提交 `aa3ec3429f4dc3d434b5808862612703661bd8b0`，Node 22 / Python 3.11 / Chromium。

| 层次 | 结果 | 口径 |
|---|---:|---|
| 网页单元与受控接口 | 116 / 116 | 保留原48项，新增约束、可读性、任务及发布合同 |
| Python适配回归 | 144 / 144 | CI补齐pymongo后无跳过；离线/受控，不是运行真实数据库 |
| 同标签PE500 | 489 / 500 | 问答289/300、工具100/100、证据60/60、旧Loop40/40 |
| 真实HTTP浏览器 | 43 / 43 | 上传、引用、八工具、复核导出、版本失效、候选发布/回滚、移动端 |
| 原代码/标签保留 | 224份代码＋2份标签 | SHA-256一致；不代表任意语义无退化 |

浏览器是Actions上的真实本地HTTP服务，不是生产站点，也不是静态截图验证。检查包括：正文要点可读、原文展开/定位、单次反馈分母；八类工具运行、未确认禁止导出、JSON含版本/审批、真实OOXML；修改输入撤销确认、导航/刷新保留；TXT/DOCX/PDF解析、115页保留；资料变化阻止旧候选发布、重验收、确认生效、下一次请求使用top-k=7并回滚至5；受控迟到模型响应不覆盖新会话；九入口和手机宽度无横向溢出。43项全部通过，无未捕获页面错误。

完整记录和截图在该运行的 `jinshu-v7-acceptance` 工件。工件SHA-256：`03b4bbcd3ad7aa25bfa08c3eab7c72e8037b88eadf405919cc58bf6bed30231b`。

## 保留失败，不替换口径

本地测试原为142项Python通过、2项缺少pymongo跳过；远端补齐依赖后144项通过。受管本地浏览器禁止HTTP访问，因此本地视觉预览不作为HTTP验收；上述Actions独立完成43项。

源代码投递第一次因README的历史文档差异未应用，第二次因Actions令牌不具备workflow写入权限未推送；随后归档精确旧README、通过连接器单独创建验收workflow，再校验提交应用源码。临时投递文件和工作流已移除，没有为通过测试关闭约束。

PE500仍保留11项问答/指定来源标签待复核；不是500个独立领域，不是模型准确率。四个已修复工具挑战不再只是未合并候选。所有新模型路径测试均为受控响应，未调用付费模型或上传私有金融原文。

## 完成范围

V7已实现可读问答、金融改写约束、真实问答配对回放、版本绑定发布、八类单工具任务生命周期和输入质量修复。原engine、pi、模型API、预算与受限策略文件按清单保留。

未在本轮完成：真实模型全链路质量试点、通用多步任务规划、历史时点制度查询、跨服务原子发布、企业多用户审批、真实用户A/B和金融业务效率测量。浏览器本地生效与完整服务灰度设计不得混用。

## 站点与源码分开

源码合并到main之后，独立私有发布仓库仍需将固定上游提交和SHA-256清单更新。站点只有其 `build-info.json` 对应新提交并通过实站复验后，才可以称为本轮已上线。后续站点记录另附，不用此次本地HTTP43项冒充线上验收。
