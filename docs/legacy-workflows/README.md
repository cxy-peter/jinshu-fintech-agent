# 历史工作流存档

这些文件保留V4–V8的历史代码恢复、模型/服务实验、Lite静态网页及旧发布验证流程。它们针对旧依赖和入口，不应与当前V9核心版混为一谈。

源文件被移出 `.github/workflows` 是为了避免误触旧代码恢复或旧站发布，不是声称旧测试全部通过。旧V7 Python3.11与新NumPy不兼容的问题仍可从历史运行查到。重跑历史版本应在对应历史commit/分支上进行。

当前V9的默认验收为 `Core V9 runtime acceptance`，手动发版为 `Manual Vercel Deploy`。
