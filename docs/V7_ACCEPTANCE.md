# V7 验收与交付边界

## 本地已执行

- 旧代码基线：网页48/48；Python126通过、2项因缺pymongo跳过。
- 新代码：网页116/116；Python142通过、2项因缺pymongo跳过。包含受控模型接口合同、任务版本、迟到结果、发布前重验收、回滚及导出期间变更。
- 冻结PE500：489/500（QA289/300、工具100/100、证据60/60、旧Loop40/40）；原100题保持通过。新治理不冒充旧40项的评测范围。
- 四个旧工具挑战修复：非法千分位、数值登记编码、两项现金流缺失。仍保留11道问答未通过，不改标签、不删除失败。

## 浏览器与来源保护

本地容器的Chromium禁止HTTP导航。保留该失败，不改变管理策略；另以about:blank载入实际模块做离线视觉检查（模拟资源加载、哈希桥接、存储回退），不冒充IndexedDB/HTTP端到端。完整界面脚本在GitHub CI用正常本地HTTP运行，实际结果以对应Actions和evidence为准。

`v7_preservation.py` 对比原引擎、pi、模型接口提示与预算的固定来源指纹；检查原PE500标签字节一致。通过只说明这些源码保留，不保证新功能没有任何语义回退。

## CI复现

运行V7 acceptance工作流：依赖安装 → 原资产保护 → 程序与Python测试 → 生成既有冻结标签 → 500项复测 → Chromium真实页面/上传/持久状态/发布回滚测试。测试仅使用公开或模拟输入及受控模型响应；不用账户密钥、真实个人资料、付款或发信。

## 不使用的指标表述

489/500不是LLM准确率；116程序检查不是116用户；本地启用不是服务器部署；文件生成不是用户采纳；已解决率必须同时列反馈覆盖。完整服务真实模型和数据库端到端、真人盲评、用户提效与线上实验仍需独立验收。

## 发布

源码、测试和公开说明可以合并；公开站点另由发布仓库固定上游版本。只有站点build-info与该版本一致且浏览器检查通过，才称站点已更新。工作区与模型配置留给原部署，不把未开启接口说成真实模型已联通。
