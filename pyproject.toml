[project]
name = "jinshu-unified-workbench"
version = "9.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "fastapi==0.128.2",
  "uvicorn==0.48.0",
  "httpx==0.28.1",
  "python-dotenv==1.2.2",
]

[tool.uv]
package = false

[tool.vercel]
entrypoint = "index:app"

[tool.vercel.fastapi.static]
cdn = false
